<header transition:name = "Main Header" transition:persist>
    <div class="center">
        <nav id="nav">
            <div class="home prevent-select">
                <a id="logo" class="prevent-select" href="/">
                    home
                    <span class="hidden">Kirati Kiviniemi</span>
                </a>
                <a href="javascript:void(0);" class="symbol hide-on-desktop" onclick="toggleLinks()">=</a>
            </div>
            <div id="links">
                <a href="/projects/">projects</a>
                <a href="/about/">about</a>
                <a href="/now/">now</a>
            </div>
            
        </nav>
    </div>
</header>

<script is:inline>
    function toggleLinks() {
        var links = document.getElementById("links");
        if (links.style.display === "flex") {
            links.style.display = "none";
        } else {
            links.style.display = "flex";
        }
    };
</script>

<style>
    svg {
        display: block;
        width: auto;
        max-inline-size: 100%;
    }

    header {
        width: 100vw;
        position: sticky;
        background-color: var(--white);
        top: 0;
        z-index: 10;
    }

    #nav {
        font-family: "Unnamed Font Copy", "Mondwest";
        display: flex;
        width: 100%;
        justify-content: space-between;
        padding-block: var(--space-s);
        font-size: var(--step-3);
        line-height: 1;
    }

    .home{
        display: flex;
        justify-content: space-between;
    }

    #links {
        display: flex;
        gap: var(--space-l);
    }

    .symbol{
        font-size: var(--step-4);
    }

    @media only screen and (max-width: 800px) {
        #nav {
            flex-direction: column
        }

        #links {
            display: none;
            flex-direction: column;
            gap: var(--space-m);
        }
    }

    @media only screen and (min-width: 800px) {
        #links {
            display: flex;
        }
    }
</style>


