<footer>
    <div class="center">
        <div class="content">
            <div class="links">
                <a href="https://instagram.com/kirati.kiviniemi">Instagram</a>
            </div>
            <div class="links">
                <a href="https://github.com/notkirati">GitHub</a>
            </div>
            <div class="banner">
                <span class="emoji">&#129505</span>
                <p>Thanks for stopping by.</p>
            </div>
            <div class="contact">
                <div class="contact-content">
                    <div class="contact-header">
                        <div>
                            <h2>Contact</h2>
                            <p>You can reach me easily by the methods below.</p>
                        </div>
                    </div>
                </div>
                <div class="information">
                    <a href="mailto:kiviniemi@kirati.dev">kiviniemi@kirati.dev</a>
                    <a href="tel:+16156636378">(615) 663-6378</a>
                </div>
            </div>
        </div>
    </div>
</footer>

<style>
    .center {
        box-sizing: content-box;
        margin-inline: auto;
        width: calc(100% - var(--space-xl) * 2);
        max-inline-size: 800px;
        padding-inline: var(--space-xl);
    }

    footer {
        font-size: var(--step--1);
        padding-block: var(--space-2xl);
        width: 100%;
        border-top: 1px solid var(--gray-6);
    }

    .content {
        display: grid;
        grid-template: "links1 links2 information" 1fr "banner banner information" auto / .5fr .5fr 2fr;
        gap: var(--space-m) var(--space-xl);
    }

    .links:first-child {
        grid-area: links1;
    }

    .links {
        display: flex;
        flex-flow: column wrap;
        gap: var(--space-2xs);
    }

    .links a {
        display: inline-block;
    }

    a {
        color: unset;
        text-decoration: underline;
        text-decoration-color: var(--gray-8);
        text-underline-offset: max(.1em,2.5px);
        transition: text-decoration-color .2s ease-in-out,font-weight .2s ease-in-out;
    }

    .banner {
        grid-area: banner;
        color: var(--gray-11);
        display: flex;
        align-items: center;
        gap: var(--space-s);
        transition: background-color .2s ease-in-out;
    }

    .banner .emoji {
        transform: scale(1.5) translateY(.04em);
    }

    .banner p {
        text-decoration: none;
    }

    .contact {
        grid-area: information;
        display: flex;
        flex-direction: column;
        margin-inline: auto;
        position: relative;
        max-width: 600px;
        gap: var(--space-m);
    }

    .contact-content {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        flex: 1;
    }

    .contact-header h2 {
        font-size: var(--step-1);
        font-weight: var(--font-weight-bold);
        line-height: var(--line-height-tight);
        margin-block-end: var(--space-2xs);
    }

    .information {
        position: relative;
        width: 100%;
    }

    p {
        overflow-wrap: break-word;
    }